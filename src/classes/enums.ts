export enum Hand {
    left,
    right,
}

export enum Note {
    A = 'A',
    B = 'B',
    C = 'C',
    D = 'D',
    E = 'E',
    F = 'F',
    G = 'G',
}

export enum NoteAlteration {
    none = '',
    sharp = '#',
    flat = 'b',
}

export enum HandpanNoteType {
    topNote,
    bottomNote,
    ding,
}